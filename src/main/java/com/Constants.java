package com;

public class Constants {
    //only one env because of that I declare it as const
    public static final String BASE_URL = "http://localhost:8080/";

    // did not add for proj gitignore
    // TestNG tags Regression, Smoke, ..
    // Logger if needed
    // Different structure of Web elements (Button, CheckBox,..)
    // Grid execution
    // Assertion Class for elements, for Matchers,
}
